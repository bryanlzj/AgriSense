# FastAPI Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.12.1

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.0.1  # Pin to compatible version with passlib
python-dotenv==1.0.0  # Environment variables

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# HTTP Client (for external APIs like OpenWeatherMap)
httpx==0.25.1
requests==2.31.0

# Image Processing (for pest detection)
Pillow==10.1.0

# Date/Time utilities
python-dateutil==2.8.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code Quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# Data Generation (for synthetic sensor data)
faker==20.1.0

# Note: PostgreSQL support (psycopg2-binary, asyncpg) will be added later
# when migrating from SQLite to PostgreSQL in production
